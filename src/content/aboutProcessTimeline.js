import { CLIENT_PROCESS_STEPS } from './clientProcessSteps'

/** Procestijdlijn voor /over-hb/onze-aanpak — generiek proces (klantreview juli 2026). */
export const ABOUT_PROCESS_STEPS = CLIENT_PROCESS_STEPS
